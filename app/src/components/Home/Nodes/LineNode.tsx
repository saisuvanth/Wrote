import { Box } from '@mui/material'
import React, { FC } from 'react'
import { ComponentNode, ILineNode } from '../../../types'

const LineNode: FC<ComponentNode> = ({ node, index }: { node: ILineNode, index: number }) => {

	return (
		<Box>

		</Box>
	)
}

export default LineNode